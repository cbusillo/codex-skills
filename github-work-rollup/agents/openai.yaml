interface:
  display_name: "GitHub Work Rollup"
  short_description: "Roll up work and unanswered comments"
  default_prompt: "Use $github-work-rollup to summarize active, blocked, waiting, ready, stale, recently completed, and unanswered external GitHub comments."

policy:
  allow_implicit_invocation: true
