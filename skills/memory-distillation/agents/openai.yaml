---
interface:
  display_name: "Memory Distillation"
  short_description: "Audit memories into durable sources"
  default_prompt: >-
    Use $memory-distillation to audit Codex and Codex Lab memories and propose
    durable promotions or retirement from reusable context.

policy:
  allow_implicit_invocation: false
