interface:
  display_name: "GitHub Work Rollup"
  short_description: "Roll up work and external comments"
  default_prompt: "Use $github-work-rollup to summarize GitHub work or find external comments that still need the owner's attention."

policy:
  allow_implicit_invocation: true
